import { StrictOmit } from 'ts-gems';
import type { DataType } from '../data-type/data-type.interface.js';
import type { Collection } from './collection.interface.js';
import type { ResourceBase } from './resource.interface.js';
export interface Singleton extends StrictOmit<ResourceBase, 'kind'> {
    kind: Singleton.Kind;
    type: DataType.Name;
    operations?: Singleton.Operations;
}
export declare namespace Singleton {
    const Kind = "Singleton";
    type Kind = 'Singleton';
    interface Operations {
        create?: Operations.Create;
        delete?: Operations.Delete;
        get?: Operations.Get;
        update?: Operations.Update;
    }
    namespace Operations {
        type Create = Collection.Operations.Create;
        type Delete = Collection.Operations.Delete;
        type Get = Collection.Operations.Get;
        type Update = Collection.Operations.Update;
    }
}
